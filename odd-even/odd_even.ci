
mainmodule odd_even {
  mainchare Main {
    entry Main(CkArgMsg *m);
    entry void done(int idx, int num);
  };

  array [1D] Elem {
    entry Elem(CProxy_Main m, int s);
    entry void recv_num(int d, int from);
  };
};
