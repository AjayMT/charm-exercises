mainmodule Primes {
  mainchare Main {
    entry Main(CkArgMsg *m);
    entry void result(int index, int m, bool results[m]);
  };

  chare PrimeChecker {
    entry PrimeChecker(int index, int m, uint64_t nums[m], CProxy_Main main);
  }
};
